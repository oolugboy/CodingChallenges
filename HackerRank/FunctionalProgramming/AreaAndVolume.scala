import scala.io.StdIn

object Solution {
  def area(a: List[Int], b: List[Int], l, r, n)
    
  def f(a: List[Int], b: List[Int], x
  def main(args: Array[String]): Unit = {
    val a = List.range(0, 5).map(x => StdIn.readInt())
    val b = List.range(0, 5).map(x => StdIn.readInt())
    val l = StdIn.readInt()
    val r = StdIn.readInt()


  }
}
